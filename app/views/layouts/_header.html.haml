%div#header
  %nav.navbar.navbar-default
    %div.container-fluid
      %div.navbar-header
        %button.navbar-toggle.collapsed{type: "button", data: {toggle:"collapse", target: "#"}}
          %span.sr-only
            Toggle navigation
          %span.icon-bar
          %span.icon-bar
          %span.icon-bar
        %a.navbar-brand{ href: "#"}
          Sogno

      %div.collapse.navbar-collapse
        %ul.nav.navbar-nav
          %li.dropdown
            %a.dropdown-toggle{ href: "#", role: "button", aria: {expanded: "false"} }
              Instance State
              %span.caret
            %ul.dropdown-menu.state-menu{role:"menu"}
              - Ec2::Instance.states.each do |state|
                %li{state: "#{state}"}
                  %a{href:"#"}= state.capitalize
        %p.navbar-text.navbar-right
          Hello #{current_user.username}!